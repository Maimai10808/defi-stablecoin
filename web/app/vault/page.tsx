export default function VaultPage() {
  return (
    <main className="mx-auto w-full max-w-5xl p-6">
      <section className="rounded-2xl border p-6">
        <h1 className="text-2xl font-semibold">Vault View</h1>
        <p className="mt-2 text-sm text-muted-foreground">
          This route is reserved for a deeper vault-focused view. The homepage
          currently contains the primary protocol demo flows.
        </p>
      </section>
    </main>
  );
}
